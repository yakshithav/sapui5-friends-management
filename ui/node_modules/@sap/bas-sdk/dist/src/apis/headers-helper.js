"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addJwtHeader = void 0;
const core_utils_1 = require("../utils/core-utils");
const ENV_H2O_JWT = "H2O_JWT";
function addJwtHeader(headers) {
    if (!(0, core_utils_1.isAppStudio)()) {
        //if it is not appstudio add the H2O_JWT to the header, so the destination API will work outside BAS environment
        headers["x-approuter-authorization"] = `Bearer ${(0, core_utils_1.getEnvValue)(ENV_H2O_JWT)}`;
    }
    return headers;
}
exports.addJwtHeader = addJwtHeader;
//# sourceMappingURL=headers-helper.js.map