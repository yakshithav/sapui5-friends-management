import * as devUtils from "../utils/devspace-utils";
export declare const BAS_PLAN = "TENANT_PLAN";
export declare const BUILD_CODE_PLAN = "BUILD_CODE_PLAN";
/**
 * Return information about the current workspace.
 *
 * @returns - devspace information
 */
export declare function getDevspaceInfo(options?: {
    landscapeUrl: string;
    jwt: string;
    wsId: string;
}): Promise<devUtils.DevspaceInfo>;
export declare function getDevSpaces(landscapeUrl: string, jwt: string): Promise<devUtils.DevspaceInfo[]>;
export declare function deleteDevSpace(landscapeUrl: string, jwt: string, wsId: string): Promise<void>;
export declare function getDevSpacesSpec(landscapeUrl: string, jwt: string): Promise<devUtils.DevSpaceSpec | undefined>;
export declare function getExtensionPacks(landscapeUrl: string, jwt: string): Promise<devUtils.ExtensionPackInfo[]>;
export declare function createDevSpace(landscapeUrl: string, jwt: string, data: devUtils.DevSpaceCreation): Promise<void>;
export declare function updateDevSpace(landscapeUrl: string, jwt: string, wsId: string, data: devUtils.DevspaceUpdate): Promise<void>;
export declare function isBuildCode(opts?: {
    buildCodePlan?: string;
    wsPlan?: string;
}): boolean;
export declare function isBuild(opts?: {
    buildCodePlan?: string;
    wsPlan?: string;
}): boolean;
export declare function getTenantMetadata(tenant: {
    landscape: string;
    jwt: string;
}): Promise<{
    buildCodePlan: string;
    wsPlan: string;
    host: string;
}>;
export declare function getBasMode(opts?: {
    buildCodePlan?: string;
    wsPlan?: string;
}): BasMode;
/**
 * Retrieves the app-external-uri-from-ds based on an internal URI.
 * @param internalUri The internal URI to extract information from.
 * @returns The final URL with the app-external-uri-from-ds encoded.
 */
export declare function getAppExternalUri(internalUri: string): string;
/**  Calculates application URL according to WS_BASE_URL and port
 * @param port
 * @scheme http or https
 * Returns the calculated url ending with '/'
 */
export declare function calculateAppURLByPort(port: number, scheme?: string): string;
export type BasMode = "personal-edition" | "standard" | "free" | "buildCodeFree" | "buildCodeStandard" | "buildStandard";
