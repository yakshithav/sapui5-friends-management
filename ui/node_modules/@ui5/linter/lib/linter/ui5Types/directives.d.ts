import ts from "typescript";
import { Directive, LintMetadata } from "../LinterContext.js";
interface PossibleDirective extends Directive {
    pos: number;
    length: number;
}
export declare function findDirectives(sourceFile: ts.SourceFile, metadata: LintMetadata): void;
export declare function collectPossibleDirectives(sourceFile: ts.SourceFile): Set<PossibleDirective>;
export {};
