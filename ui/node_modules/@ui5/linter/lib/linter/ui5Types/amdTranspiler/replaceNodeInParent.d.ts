import ts from "typescript";
export interface NodeReplacement {
    original: ts.Node;
    substitute: ts.Node;
}
/**
 * Replaces a node in its parent with another node
 */
export default function (parent: ts.Node, replacement: NodeReplacement, nodeFactory: ts.NodeFactory): ts.Node;
