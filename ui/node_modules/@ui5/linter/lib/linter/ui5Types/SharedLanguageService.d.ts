import ts from "typescript";
export default class SharedLanguageService {
    private readonly languageServiceHostProxy;
    private readonly documentRegistryProxy;
    private readonly languageService;
    private acquired;
    private projectScriptVersion;
    constructor();
    acquire(languageServiceHost: ts.LanguageServiceHost): void;
    getProgram(): ts.Program;
    release(): void;
    getNextProjectScriptVersion(): string;
}
