import { Resource } from "@ui5/fs";
import { RawLintMessage } from "../linter/LinterContext.js";
import { ChangeSet } from "./autofix.js";
export default function generateChangesXml(messages: RawLintMessage[], changeSets: ChangeSet[], content: string, resource: Resource): Promise<void>;
