
"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

var requireIndex = require("requireindex");

//------------------------------------------------------------------------------
// Plugin Definition
//------------------------------------------------------------------------------


// import all rules in lib/rules
module.exports.rules = requireIndex(__dirname + "/rules");

// add link to documentation for each rule
// var baseUrl = "https://github.com/SAPSE/eslint-plugin-fiori-custom/blob/master/docs/rules/";
var baseUrl = "https://www.npmjs.com/package/eslint-plugin-fiori-custom#";
var createNestedObject = function (base, names, value) {
    for (var i = 0; i < names.length - 1; i++) {
        base = base[names[i]] = base[names[i]] || {};
    }
    base[names[names.length - 1]] = value;
};
for (var property in module.exports.rules) {
    createNestedObject(module.exports.rules[property], ["meta", "docs", "url"], baseUrl + property + ".md");
}
module.exports.configs = {
    default: {
        extends:"../configure.eslintrc"
    },
	testcode: {
		extends:"../testcode.eslintrc"
	},
	fioriToolsDefault: {
		extends:"../fiori_tools_configure.eslintrc"
	},
	fioriToolsTestcode: {
		extends:"../fiori_tools_testcode.eslintrc"
	}
};